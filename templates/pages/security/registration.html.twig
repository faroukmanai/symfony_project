
{% extends "base.html.twig" %}

{% block title %}
	MyRecipe | Inscription
{% endblock %}

{% block body %}
	<div class="container">
        {% for message in app.flashes('success') %}
            <div class=" alert alert-success">
                {{message}}
            </div>
        {% endfor %}
		<h1 class="mt-4">Formulaire d'inscription</h1>

		{{ form(form) }}

		</div>
	{% endblock %}
